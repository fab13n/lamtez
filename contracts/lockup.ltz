@release_date :: time
@amount :: tez
@destination :: contract unit unit

\(parameter::unit): ( now >= @release_date ?
                    | False: fail
                    | True: contract-call () @amount @destination
                    )
